{
  "name": "linkedin-headline-audit",
  "version": "1.0.0",
  "description": "LinkedIn headline audit tool — The MVB Lab",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
